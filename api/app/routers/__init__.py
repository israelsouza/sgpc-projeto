from fastapi import APIRouter

router = APIRouter()

# Importe os roteadores aqui conforme forem criados, ex:
# from app.routers.users import router as users_router
# router.include_router(users_router, prefix="/users", tags=["Users"])
